ЗАДАЧА: спроектировать и собрать общий AI-репозиторий, из которого Claude Code тянет
harness, memory и agents в любом проекте.

КОНТЕКСТ / ТЕКУЩЕЕ СОСТОЯНИЕ
- Создан приватный репозиторий /Volumes/SSD/Dev/ai (remote git@github.com:w3con/ai.git,
  пока только коммит "init"; внутри уже есть harness/, memory/, violations/, .gitignore).
- Это должен стать ЕДИНЫЙ дом для всего, что касается AI и не привязано к конкретному
  проекту: весь harness (constraints/violations/gates/enforcement/operator-guide),
  вся память о юзере (user-level), вся кросс-проектная память и задачи про AI, агенты,
  глобальные правила/настройки.
- Сейчас это разбросано и лежит неправильно: харнес — в проектной папке
  Validité/ai/harness/ (это ошибка, его надо вынести); user-память — в ~/.claude/memory/;
  агент scope — в ~/.claude/agents/; глобальный CLAUDE.md — в ~/.claude/CLAUDE.md;
  планы — в ~/.claude/plans/.

ЦЕЛЬ (как должно стать)
- /Volumes/SSD/Dev/ai = источник истины для всего AI-глобального, синхронизируется между
  машинами через git (репо w3con/ai).
- Запуск Claude Code в ЛЮБОЙ проектной папке должен подтягивать harness, memory и agents
  ИМЕННО из /Volumes/SSD/Dev/ai — а НЕ из ~/.claude/.
- ~/.claude/plans/ должен быть пуст: глобальные/AI-планы уезжают в общий репо, проектные —
  в папку проекта.
- Проектные папки (напр. /Volumes/Documents/startup/Validité) хранят ТОЛЬКО проектное:
  задачи, планы, сессии и память по этому проекту.

МЕХАНИЗМ (предложить и подтвердить)
- Claude Code читает правила/память из ~/.claude/ (CLAUDE.md, memory/, agents/) — значит,
  общий репо надо завести в ~/.claude/ симлинками: репо = источник истины, а
  ~/.claude/{CLAUDE.md,memory,agents,harness,…} — симлинки внутрь /Volumes/SSD/Dev/ai.
- Симлинки создаются на каждой машине отдельно (путь к репо на 2-й машине другой, сами
  симлинки в git не едут) — нужен одноразовый bootstrap-скрипт/инструкция на клон.
- Учесть риск: репо на /Volumes/SSD — если том не примонтирован, Claude останется без
  правил/памяти.

ОТКРЫТЫЕ ВОПРОСЫ К РЕШЕНИЮ
- Что именно входит в "весь харнес": только harness/+memory/+agents/, или ещё skills/,
  settings.json, statusline?
- Куда деть ai/templates/kb-skeleton (кросс-проектный тулинг) — в общий репо или оставить
  в проекте?
- Обновить агент scope, чтобы он больше НЕ писал в ~/.claude/plans/, а маршрутизировал:
  проектные планы → папка проекта, AI-общие → общий репо.
- Перенести harness из Validité/ai/harness/ в /Volumes/SSD/Dev/ai/harness/ и вычистить
  глобальное из проектной папки.

ВАЖНО
- Перед сборкой — план по фазам на диск, показать, утвердить, и только потом реализация
  (Opus планирует, Sonnet реализует).
- Прошлый разбор по слиянию проектной памяти Validité (live-папка vs ai/memory) — это
  отдельная, ПРОЕКТНАЯ задача, она остаётся в Validité и здесь не смешивается.
