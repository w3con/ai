---
name: feedback_web_repo_boundary
description: Do not read the separate web repo unless explicitly asked
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 88565687-8a92-4e3e-8bc0-989c446c688d
---

Do not open or read the separate website/pitch repo at `/Volumes/SSD/Dev/validite/...` (the validite.eu site + pitch decks) unless the user explicitly asks. Stay within the KB repo `/Volumes/Documents/startup/Validité` by default.

**Why:** The user considers cross-repo reading out of scope for normal KB work; the two are separate stores and he drives when the web repo is relevant. He explicitly rejected the framing that I "should have" read the web repo on my own.

**How to apply:** When answering positioning/messaging questions, work from the Outline KB cache (`.ai/docs/`). If the current frame seems to live elsewhere (e.g. site/decks), say so and ask before reading the web repo. Note the related root issue: the authoritative narrative diverged between the Outline KB (stale) and the web repo (current) — see [[feedback_strategy_capture]].
